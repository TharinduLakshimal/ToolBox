# ToolBox Project

A full-stack application featuring a React frontend, Java (Spring Boot) backend, and MySQL database, all containerized with Docker.

## 🚀 How to Run
1. **Prerequisites**: Ensure you have [Docker Desktop](https://www.docker.com/products/docker-desktop/) installed.
2. **Clone the repo**:
   ```bash
   git clone [https://github.com/TharinduLakshimal/ToolBox.git](https://github.com/TharinduLakshimal/ToolBox.git)
   cd ToolBox
3. **Launch the stack**:

    ```bash
    docker-compose up --build
4. **Access the App**:

    Frontend: http://localhost:3000

    Backend API: http://localhost:8080